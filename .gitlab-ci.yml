##
stages:
  - build
  - sonar
  - test

build:
  stage: build
  script: ./gradlew assemble

sonar:
  stage: sonar
  script: ./gradlew sonarqube -D sonar.projectKey=getandgo -D sonar.host.url=%URL% -D sonar.login=%SONARKEY%

test:
  stage: test
  script: ./gradlew test


after_script:
  - echo "End CI~!"