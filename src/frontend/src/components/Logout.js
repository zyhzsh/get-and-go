import React from 'react'

const Logout = () => {
    return (
        <div>
            <h1>Welcome <span>blabla</span>
            <button>Logout</button>
            </h1>
        </div>
    )
}

export default Logout
